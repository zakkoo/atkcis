<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ATK Check-in Desk</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="~/css/app.min.css" rel="stylesheet" asp-append-version="true">
    <link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">
</head>
<body class="min-h-screen bg-black text-terminal-glow font-terminal">
    <div class="min-h-screen px-6 py-8 md:px-12">
        <header class="mb-10 flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
            <div class="text-3xl tracking-[0.3em] text-terminal-glow/90">
                <a class="transition hover:text-terminal-glow" asp-page="/Index">ATK - Check-in Desk</a>
            </div>
            <nav class="text-sm uppercase tracking-[0.2em] text-terminal-glow/70">
                <ul class="flex flex-wrap gap-6">
                    <li><a class="transition hover:text-terminal-glow" asp-page="/Index">Home</a></li>
                    <li><a class="transition hover:text-terminal-glow" asp-page="/SignUp">Sign up</a></li>
                    <li><a class="transition hover:text-terminal-glow" asp-page="/CheckIn">Check me in</a></li>
                    <li><a class="transition hover:text-terminal-glow" asp-page="/CheckOut">Check me out</a></li>
                    <li><a class="transition hover:text-terminal-glow" asp-page="/GetBarcode">My Barcode</a></li>
                    <li>
                        <details class="group relative">
                            <summary class="cursor-pointer list-none text-terminal-glow/70 transition hover:text-terminal-glow">
                                Admin
                            </summary>
                            <ul class="mt-2 hidden flex-col gap-2 text-terminal-glow/60 group-hover:flex group-open:flex">
                                <li><a class="transition hover:text-terminal-glow" asp-page="/Admin/Cleanup">Cleanup</a></li>
                                <li><a class="transition hover:text-terminal-glow" asp-page="/Admin/Data">Data</a></li>
                            </ul>
                        </details>
                    </li>
                </ul>
            </nav>
        </header>
        <main role="main" class="max-w-3xl space-y-6 text-base leading-relaxed text-terminal-glow/80">
            @RenderBody()
        </main>
    </div>
</body>
</html>
